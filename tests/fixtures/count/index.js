const emptyAnimals = require('./emptyAnimals')
const noAnimals = require('./noAnimals')
const emptyPeople = require('./emptyPeople')
const noPeople = require('./noPeople')

module.exports = {
    emptyAnimals,
    noAnimals,
    emptyPeople,
    noPeople
}
